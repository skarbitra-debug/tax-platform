{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // бот — чистый Node-процесс: типы Node (process, console), без DOM
    "types": ["node"]
  },
  "include": ["src", "tsup.config.ts"]
}
