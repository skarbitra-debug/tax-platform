# B02a.2 — TypeScript lint и измеренный baseline

В корне добавлены точные dev-инструменты ESLint9.39.5, @eslint/js9.39.5, typescript-eslint8.70.0 и globals16.5.0. Runtime версии, включая Next15.5.24, Auth, React и Prisma, сохранены. ESLint9.39.5 помечен registry как неподдерживаемый; это ограничение согласованного toolchain, обновление major в пакет не входит.

`pnpm lint:check` запускает `eslint . --max-warnings 0`. Flat config применяет JS recommended и TypeScript recommended без type-aware/stylistic/Next/React-hooks правил. `no-undef` остаётся активным для JS, а для TS/TSX проверка принадлежит сохранённому tsc. Неиспользуемые переменные — ошибки; только имена с начальным underscore допускаются для намеренно неиспользуемых аргументов/переменных/ошибок.

Исключены node_modules, .next, dist, generated, coverage, Playwright reports/test-results и next-env.d.ts. Tests, auth, старые client forms и целые workspace-зоны не исключались. Исходный корпус114 tracked файлов и новый config дали115 lint results. Девять representative print-config подтвердили TS parser/rules во всех зонах; generated client исключён, JS no-undef активен.

## Исходная диагностика

На input d93b03764bec09f4e574f5279e08eb3f639be118 получены3errors,0warnings,0fatal:

| Файл | Строка | Правило |
|---|---|---|
| apps/web/src/app/(marketing)/earnings-calculator.tsx |70,71| no-irregular-whitespace |
| packages/core/src/status/engine.ts |93| @typescript-eslint/no-unused-vars: toStatusCode |

Полный lint вернул exit1; Next build также остановился на двух whitespace errors. Диагностика не скрывается rules/ignores/ignoreDuringBuilds. По отдельному scoped решению root оба literal NBSP заменены явными Unicode escape expressions с тем же U+00A0; unused binding переименован в _toStatusCode с сохранением ...rest. Decoded strings совпадают с исходными текстами; статусная логика не менялась. После исправлений115 файлов дают0errors/0warnings/0fatal. Root lint активирован как pnpm lint:check, существующий CI pnpm lint теперь действительно проверяет корпус. Отдельная проба через root script дала RED, после удаления — GREEN.

Пять отдельных временных TS probes (web,bot,core,integration,e2e) проверили no-explicit-any/unused-var RED, затем экспорт корректного значения дал GREEN. TSX с JSX automatic runtime прошёл; syntax-invalid TS дал parsing RED. Все собственные probes удалены, в commit не включаются.

Type-aware lint, Next-specific/React-hooks правила и форматирование не входят в этот срез. Next build предупреждает об отсутствии Next ESLint plugin; отдельный полный root lint обеспечивает охват остальных workspace. Remote CI, публикация, deployment и работа с реальной БД не выполняются этим пакетом.
